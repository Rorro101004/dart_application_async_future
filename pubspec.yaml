name: dart_application_api
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.9.3

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  http: ^1.6.0
  retry: ^3.1.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
